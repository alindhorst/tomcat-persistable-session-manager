<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.alexanderlindhorst</groupId>
        <artifactId>tomcat-persistable-session-manager</artifactId>
        <version>1.1-SNAPSHOT</version>
    </parent>
    <artifactId>sessiontest</artifactId>
    <packaging>war</packaging>

    <name>Tomcat PersistableSession Manager (SessionTest WebApp)</name>

    <properties>
        <endorsed.dir>${project.build.directory}/endorsed</endorsed.dir>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <memcached-session-manager-version>1.8.3</memcached-session-manager-version>
        <spymemcached-version>2.11.1</spymemcached-version>
        <msm-kryo-serializer-version>1.8.0</msm-kryo-serializer-version>
        <!-- Maven related -->
        <maven.test.skip>true</maven.test.skip>
        <cobertura.skip>true</cobertura.skip>
    </properties>
    
    <dependencies>
        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-web-api</artifactId>
            <version>7.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>sessiontest</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <compilerArguments>
                        <endorseddirs>${endorsed.dir}</endorseddirs>
                    </compilerArguments>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.3</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <packagingExcludes>WEB-INF/lib/*.jar</packagingExcludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.6</version>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${endorsed.dir}</outputDirectory>
                            <silent>true</silent>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>javax</groupId>
                                    <artifactId>javaee-endorsed-api</artifactId>
                                    <version>7.0</version>
                                    <type>jar</type>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>cobertura-maven-plugin</artifactId>
                <version>2.7</version>
                <executions>
                    <execution>
                        <id>instrument-code</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>instrument</goal>
                        </goals>
                        <configuration>
                            <attach>false</attach>
                            <skip>true</skip>
                            <instrumentation>
                                <ignores>
                                    <ignore>*</ignore>
                                </ignores>
                                <excludes>
                                    <exclude>**/*.class</exclude>
                                </excludes>
                            </instrumentation>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
                        -->
        </plugins>
    </build>
    <profiles>
        <profile>
            <activation>
                <property>
                    <name>buildDockerImage</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.spotify</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <version>0.3.8</version>
                        <executions>
                            <execution>
                                <id>build</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                                <configuration>
                                    <imageName>sessiontest</imageName>
                                    <dockerDirectory>src/docker</dockerDirectory>
                                    <resources>
                                        <resource>
                                            <targetPath>/usr/local/tomcat/webapps</targetPath>
                                            <directory>${project.build.directory}</directory>
                                            <include>${project.build.finalName}.war</include>
                                        </resource>
                                        <resource>
                                            <targetPath>/usr/local/tomcat/lib</targetPath>
                                            <directory>${project.build.directory}/sessiontest/WEB-INF/lib</directory>
                                            <include>%regex[.*shaded.*.jar]</include>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
            <dependencies>
                <dependency>
                    <groupId>de.alexanderlindhorst</groupId>
                    <artifactId>tomcat-persistable-session-manager-riak-backend</artifactId>
                    <version>1.1-SNAPSHOT</version>
                    <scope>runtime</scope>
                    <classifier>shaded</classifier>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

</project>
